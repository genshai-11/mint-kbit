# KBIT admin app — Vercel environment template
# Upload values to the separate Vercel project for /admin.
# Browser admin app also uses only public Supabase URL + anon/publishable key.
# Privileged actions go through Supabase Edge Functions that hold server-side secrets.

VITE_SUPABASE_URL=https://skfpnghxfzipvmkrmnva.supabase.co
VITE_SUPABASE_ANON_KEY=REPLACE_WITH_SUPABASE_PUBLISHABLE_OR_ANON_KEY
